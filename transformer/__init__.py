"""CS336 course code (assignment2-systems / cs336-basics).

Two changes: the package name in model.py's import, and generate() collecting its output tokens
as it goes so that sampling past context_length returns everything it generated.
"""
